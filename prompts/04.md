# Duplicated email validation

We have the same email validation logic copy-pasted in four places under lib/anti-patterns/duplicated_validation. Extract it into a shared module and update all the call sites to use it.
